export default () => ({
    // Enable internationalization plugin
    i18n: {
        enabled: true,
        config: {
            defaultLocale: 'en',
            locales: ['en', 'fr'],
        },
    },
});
